document.addEventListener('DOMContentLoaded', () => {
  const list = document.getElementById('agent-list');
  list.innerHTML = '<p style="color:#9aa0a6">No agents yet. This is a portable demo UI.</p>';
});
